function fruit (fruit, w, pricePerKg) {

    let wKg = w/1000;

    let money = pricePerKg * w;


console.log (`I need $${(money / 1000).toFixed (2)} to buy ${wKg.toFixed (2)} kilograms ${fruit}.`);
}
fruit ('orange', 2500, 1.80);
fruit ('apple', 1563, 2.35);
